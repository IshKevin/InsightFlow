[tool.black]
line-length = 88
target-version = ["py310", "py311", "py312"]
exclude = '''
/(
    \.git
  | \.venv
  | migrations
)/
'''

[tool.isort]
profile = "black"
skip = ["migrations"]

[tool.bandit]
exclude_dirs = ["tests", "qa"]
skips = ["B101"]  # allow assert in non-test code
